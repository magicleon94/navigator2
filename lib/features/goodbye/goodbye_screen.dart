import 'package:flutter/material.dart';

class GoodbyeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Goodbye screen'),
      ),
    );
  }
}
