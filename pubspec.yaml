name: navigator2
description: Experimentig with Navigator 2.0
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation:
  flutter_bloc:
  http_services:
  pedantic:
  flow_builder: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^0.14.1+2
  build_runner:

flutter:
  uses-material-design: true
